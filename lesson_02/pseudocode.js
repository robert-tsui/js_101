// START 

// a function that returns the sum of two numbers

// SET results = value of summing two numbers

// PRINT results

// END

-------------------------------

// START

// a function that takes an array of strings, and returns a string that is all those strings concatenated together

// SET results = value of concatenating strings together

// WHILE iterator <= length of array
  // SET iterator = 0
  // add the string at each index of the array to results
  // iterator++

// PRINT results

// END

-------------------------------

// START

// a method that takes an array of integers, and returns a new array with every other element from the original array, starting with the first element. 

// SET results = a new array with every other element of the original array
// SET currentElement = current element of input array

// WHILE iterator <= length of input array
  // SET iterator = 1
  // IF currentElement is not divisible by 2
    // push currentElement to results 

// PRINT results 


-------------------------------

// START

// INPUT: string
// OUTPUT: string

/*
A function that determines the index of the 3rd occurrence of a given character in a string. 

For instance, if the given character is 'x' and the string is 'axbxcdxex',

the function should return 6 (the index of the 3rd 'x'). 

If the given character does not occur at least 3 times, return null.
*/

// SET result = index of the 3rd 'x'

// WHILE iterator <= 3
  // SET iterator = 0
  // SET counter = 0
  // IF current index is equal to char
    // increment counter

// PRINT result at the 3rd occurence of char

-------------------------------

// START

// INPUT: 2 arrays
// OUTPUT: 1 array

/*
a function that takes two arrays of numbers and returns the result of merging the arrays. 
The elements of the first array should become the elements at the even indexes of the returned array, 
while the elements of the second array should become the elements at the odd indexes.
*/

// SET result = new array that represents the merging of the two input arrays


// WHILE iterator <= length of second input array
  // SET iterator = 0
  // push to result array
  // WHILE iterator <= length of first input array
    // SET iterator = 0
    // push to result array

// PRINT result



